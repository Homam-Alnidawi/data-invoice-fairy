# ---------------------------------------------------------------
# PUBLIC (safe to expose to the browser bundle)
# ---------------------------------------------------------------
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Same values, read by the server runtime
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_PUBLISHABLE_KEY=

# ---------------------------------------------------------------
# SERVER-ONLY SECRETS — never prefix these with VITE_
# never commit real values, never read them from client code
# ---------------------------------------------------------------
SUPABASE_SERVICE_ROLE_KEY=

# AI provider (gemini | openai | anthropic | custom)
AI_PROVIDER=
AI_API_KEY=
AI_MODEL=
AI_BASE_URL=

# Optional: only while still running on the Lovable AI Gateway
LOVABLE_API_KEY=

# Encryption key for stored payment provider secrets (existing system)
PAYMENT_SECRETS_KEY=
